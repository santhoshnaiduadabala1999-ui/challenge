<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Agriculture Dashboard</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f8f5;
      color: #333;
    }

    header {
      background-color: #4CAF50;
      color: white;
      padding: 20px;
      text-align: center;
    }

    .container {
      display: flex;
      flex-wrap: wrap;
      padding: 20px;
      gap: 20px;
      justify-content: center;
    }

    .card {
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      padding: 20px;
      width: 300px;
    }

    .card h3 {
      margin-top: 0;
      color: #2e7d32;
    }

    .weather, .soil, .recommendation {
      margin-top: 10px;
    }

    footer {
      text-align: center;
      padding: 10px;
      background-color: #4CAF50;
      color: white;
      position: fixed;
      width: 100%;
      bottom: 0;
    }

    button {
      background-color: #4CAF50;
      border: none;
      color: white;
      padding: 10px 16px;
      text-align: center;
      font-size: 14px;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 10px;
    }

    button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>

  <header>
    <h1>🌱 Agriculture Dashboard</h1>
    <p>Monitor weather, soil, and crop recommendations</p>
  </header>

  <div class="container">
    <!-- Weather Card -->
    <div class="card">
      <h3>Weather</h3>
      <div class="weather">
        <p><strong>Temperature:</strong> <span id="temp">--</span> °C</p>
        <p><strong>Humidity:</strong> <span id="humidity">--</span>%</p>
        <p><strong>Condition:</strong> <span id="condition">--</span></p>
        <button onclick="loadWeather()">Refresh Weather</button>
      </div>
    </div>

    <!-- Soil Moisture Card -->
    <div class="card">
      <h3>Soil Moisture</h3>
      <div class="soil">
        <p><strong>Moisture Level:</strong> <span id="moisture">--</span>%</p>
        <button onclick="simulateMoisture()">Simulate Reading</button>
      </div>
    </div>

    <!-- Crop Recommendation Card -->
    <div class="card">
      <h3>Crop Recommendation</h3>
      <div class="recommendation">
        <p><strong>Recommended Crop:</strong> <span id="crop">--</span></p>
        <button onclick="getCropRecommendation()">Get Recommendation</button>
      </div>
    </div>
  </div>

  <footer>
    © 2025 Smart Agro Dashboard
  </footer>

  <script>
    // Simulate weather data
    function loadWeather() {
      const weatherConditions = ["Sunny", "Rainy", "Cloudy", "Windy", "Stormy"];
      const temp = (Math.random() * 15 + 20).toFixed(1); // 20°C to 35°C
      const humidity = Math.floor(Math.random() * 50 + 30); // 30% to 80%
      const condition = weatherConditions[Math.floor(Math.random() * weatherConditions.length)];

      document.getElementById("temp").innerText = temp;
      document.getElementById("humidity").innerText = humidity;
      document.getElementById("condition").innerText = condition;
    }

    // Simulate soil moisture
    function simulateMoisture() {
      const moisture = Math.floor(Math.random() * 60 + 20); // 20% to 80%
      document.getElementById("moisture").innerText = moisture;
    }

    // Crop recommendation logic
    function getCropRecommendation() {
      const crops = ["Wheat", "Maize", "Rice", "Soybean", "Cotton", "Barley"];
      const recommended = crops[Math.floor(Math.random() * crops.length)];
      document.getElementById("crop").innerText = recommended;
    }

    // Load default values on startup
    window.onload = function () {
      loadWeather();
      simulateMoisture();
      getCropRecommendation();
    };
  </script>

</body>
</html>
